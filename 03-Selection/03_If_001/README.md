<p align="left">
  <a href="../README.md">
    <img src="../../Z99-OTHERS/00-common/00-back.png" style="width:10%">
  </a>
</p>

<div align="center">
  <h1>
    Median 5 (Flowchart) ★ (
      <a href="https://drive.google.com/file/d/1d5UnfelndV7lFHFTRvd7f7u0Ydoza1Be/view?usp=drive_link">
        <code>03_If_001</code>
      </a>
    )
  </h1>
</div>

# Contents

-   [**มัธยฐานและการสลับค่า**](#มัธยฐานและการสลับค่า)
-   [**การคัดตัวเลือกตามผังงาน**](#การคัดตัวเลือกตามผังงาน)
-   [**ตัวอย่างการไล่ค่า**](#ตัวอย่างการไล่ค่า)
-   [**Solution**](#solution)

---

## มัธยฐานและการสลับค่า

มัธยฐานของข้อมูล 5 จำนวน คือจำนวนที่อยู่ตรงกลางเมื่อเรียงข้อมูลจากน้อยไปมาก
หรือก็คือจำนวนลำดับที่ 3 นั่นเอง โจทย์รับจำนวนเต็ม `a`, `b`, `c`, `d` และ
`e` แยกกันคนละบรรทัด แล้วให้ทำงานตามผังงานที่กำหนด

ผังงานไม่ได้เรียงข้อมูลทั้ง 5 จำนวนโดยตรง แต่ค่อย ๆ เปรียบเทียบและตัดจำนวนที่ไม่มี
โอกาสเป็นมัธยฐานออก การเปรียบเทียบคู่แรกเขียนได้ดังนี้

```python
if a > b:
    a, b = b, a
```

หลังคำสั่งนี้จะได้ความสัมพันธ์ $a \le b$ เสมอ คำสั่ง `a, b = b, a`
เรียกว่า **การสลับค่าแบบพร้อมกัน** โดย Python จะอ่านค่าทางขวาทั้งคู่ก่อน
แล้วจึงนำไปเก็บทางซ้าย จึงไม่ทำให้ค่าเดิมสูญหาย เช่น ถ้า `a = 40` และ
`b = 30` จะได้ `a = 30` และ `b = 40`

วิธีเดียวกันถูกใช้กับคู่ `c` และ `d` ด้วย การใช้ `>` แทน `>=` หมายความว่า
ถ้าค่าสองตัวเท่ากันจะไม่สลับตำแหน่ง ซึ่งไม่กระทบคำตอบ ค่าซ้ำ ค่า 0
และจำนวนลบก็ใช้การเปรียบเทียบแบบเดียวกันได้ทั้งหมด

## การคัดตัวเลือกตามผังงาน

ลำดับการทำงานของโปรแกรมตรงกับลูกศรในผังงาน ดังนี้

1. จัดคู่ `a, b` และ `c, d` ให้สมาชิกตัวแรกของแต่ละคู่ไม่มากกว่าสมาชิกตัวที่สอง
2. เปรียบเทียบ `a` กับ `c` แล้วปรับ `b`, `c` และ `d` เพื่อเก็บเฉพาะ
   ค่าที่ยังมีโอกาสเป็นมัธยฐาน
3. นำจำนวนที่ห้า `e` มาใส่ใน `a` ด้วย `a = e` แล้วเปรียบเทียบกับ `b`
4. เปรียบเทียบ `c` กับค่า `a` ปัจจุบัน หาก `c > a` จะสลับ `b` กับ `d`
   ก่อนกำหนด `a = c`
5. ในตอนท้าย `a` และ `d` คือผู้สมัครสองตัวสุดท้าย ผังงานจึงแสดงค่าที่น้อยกว่า
   ซึ่งมีผลเหมือนนิพจน์ `min(a, d)`

> [!NOTE]
>
> ทุกเงื่อนไขอ่านค่าล่าสุดจากขั้นก่อนหน้า เช่น หลัง `a = e` การตรวจ
> `if a > b` จะเปรียบเทียบ `e` กับ `b` ไม่ได้ใช้ค่า `a` เดิมจากข้อมูลนำเข้า

## ตัวอย่างการไล่ค่า

พิจารณาข้อมูล `40`, `30`, `20`, `50` และ `10` ตามลำดับ

| ขั้นตอน | `a` | `b` | `c` | `d` |
|---|---:|---:|---:|---:|
| รับสี่ค่าแรก | 40 | 30 | 20 | 50 |
| สลับ `a` กับ `b` | 30 | 40 | 20 | 50 |
| ปรับค่าเมื่อ `a > c` | 30 | 50 | 30 | 40 |
| กำหนด `a = e` | 10 | 50 | 30 | 40 |
| ปรับค่าเมื่อ `c > a` | 30 | 40 | 30 | 50 |

ขั้นสุดท้าย `a = 30` และ `d = 50` จึงแสดงค่าที่น้อยกว่า คือ `30`
ซึ่งเป็นมัธยฐานของข้อมูลทั้งห้าจำนวนพอดี

---

# Solution

```python
# --------------------------------------------------
# File Name : 03_If_001.py
# Problem   : Median 5 (Flowchart)
# Author    : Worralop Srichainont
# Date      : 2025-06-10
# --------------------------------------------------

a = int(input())
b = int(input())
c = int(input())
d = int(input())
e = int(input())

if a > b:
    a, b = b, a

if c > d:
    c, d = d, c

if a > c:
    b, d = d, b
    c = a

a = e

if a > b:
    a, b = b, a

if c > a:
    b, d = d, b
    a = c

if a > d:
    print(d)
else:
    print(a)
```
